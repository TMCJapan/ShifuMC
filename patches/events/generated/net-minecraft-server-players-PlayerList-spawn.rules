# PlayerInitialSpawnEvent(PlayerSpawnLocationEvent)。出る場所が決まった直後。

file: net/minecraft/server/players/PlayerList.java
anchor:
        player.setLevel(serverLevel);
insert-after:
    dev.shifu.event.PlayerEvents.initialSpawn(player);
