# PreSpawnerSpawnEvent。スポナーが実体を作る直前。

file: net/minecraft/world/level/BaseSpawner.java
anchor:
    Entity entity = EntityType.loadEntityRecursive(entityToSpawn, world, var6x -> {
insert:
    if (optional.isPresent()
            && !dev.shifu.event.EntityEvents.preSpawnerSpawn(world, optional.get(), d, d1, d2, pos)) {
        continue;
    }
