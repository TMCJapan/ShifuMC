# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# ExplosionPrimeEvent
file: net/minecraft/world/entity/vehicle/MinecartTNT.java
anchor:
    serverLevel.explode(
insert:
    // CraftBukkit start
    org.bukkit.event.entity.ExplosionPrimeEvent event = new org.bukkit.event.entity.ExplosionPrimeEvent(
        this.getBukkitEntity(),
        (float) (this.explosionPowerBase + this.explosionSpeedFactor * this.random.nextDouble() * 1.5 * min),
        this.isIncendiary
    );
    if (!event.callEvent()) {
        this.fuse = -1;
        return;
    }
    // CraftBukkit end
