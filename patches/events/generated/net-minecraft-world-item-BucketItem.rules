# PlayerBucketEmptyEvent。バケツの中身を置く直前。

file: net/minecraft/world/item/BucketItem.java
anchor:
    if (this.emptyContents(user, world, blockPos2, playerPovHitResult)) {
insert:
    if (!dev.shifu.event.PlayerEvents.bucketEmpty(user, world, blockPos2, playerPovHitResult, itemStack, hand)) {
        return InteractionResultHolder.fail(itemStack);
    }
