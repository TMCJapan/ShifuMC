# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# callEntityChangeBlockEvent
file: net/minecraft/world/item/EnderEyeItem.java
anchor:
    Block.pushEntitiesUp(blockState, blockState2, level, blockPos);
insert:
    // Paper start
    if (!org.bukkit.craftbukkit.event.CraftEventFactory.callEntityChangeBlockEvent(context.getPlayer(), blockPos, blockState2)) {
        return InteractionResult.PASS;
    }
    // Paper end
