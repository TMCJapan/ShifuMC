# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# callBinaryRedstoneChange
file: net/minecraft/world/level/block/LeverBlock.java
anchor:
    this.pull(stateBefore, level, pos, null);
insert:
    if (dev.shifu.event.EventGuard.listening(org.bukkit.event.block.BlockRedstoneEvent.class)) { // Shifu - 登録が無ければ組み立てない
        // Paper start - Call BlockRedstoneEvent
        boolean wasPowered = stateBefore.getValue(POWERED);
        if (!org.bukkit.craftbukkit.event.CraftEventFactory.callBinaryRedstoneChange(level, pos, !wasPowered)) {
            return InteractionResult.SUCCESS;
        }
        // Paper end - Call BlockRedstoneEvent
    }

# callBinaryRedstoneChange
file: net/minecraft/world/level/block/LeverBlock.java
anchor:
    this.pull(state, level, pos, null);
insert:
    // Paper start - Call BlockRedstoneEvent
    if (!dev.shifu.event.EventGuard.listening(org.bukkit.event.block.BlockRedstoneEvent.class) || org.bukkit.craftbukkit.event.CraftEventFactory.callBinaryRedstoneChange(level, pos, !state.getValue(POWERED))) {
    // Paper end - Call BlockRedstoneEvent
insert-after:
    } // Paper - Call BlockRedstoneEvent
