# PlayerAdvancementCriterionGrantEvent。条件が 1 つ埋まった直後。

file: net/minecraft/server/PlayerAdvancements.java
anchor:
        if (orStartProgress.grantProgress(criterionName)) {
insert-after:
    if (!dev.shifu.event.PlayerEvents.advancementCriterion(this.player, advancement, criterionName)) {
        orStartProgress.revokeProgress(criterionName);
        return false;
    }
