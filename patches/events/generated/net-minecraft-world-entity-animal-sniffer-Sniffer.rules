# main(26.2)の規則を reanchor.py で局所変数の名前だけ当て直したもの。

# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# EntityDropItemEvent
file: net/minecraft/world/entity/animal/sniffer/Sniffer.java
anchor:
    world.addFreshEntity(itemEntity);
insert:
    // CraftBukkit start - handle EntityDropItemEvent
    if (dev.shifu.event.EventGuard.listening(org.bukkit.event.entity.EntityDropItemEvent.class)) {
        org.bukkit.event.entity.EntityDropItemEvent event = new org.bukkit.event.entity.EntityDropItemEvent(this.getBukkitEntity(), (org.bukkit.entity.Item) itemEntity.getBukkitEntity());
        org.bukkit.Bukkit.getPluginManager().callEvent(event);
        if (event.isCancelled()) {
            return;
        }
    }
    // CraftBukkit end

# callEntityFertilizeEggEvent
file: net/minecraft/world/entity/animal/sniffer/Sniffer.java
anchor:
    ItemStack itemStack = new ItemStack(Items.SNIFFER_EGG);
insert:
    // Paper start - Add EntityFertilizeEggEvent event
    // callEntityFertilizeEggEvent は経験値のぶんの getRandom().nextInt(7) + 1 を
    // 先に引く。登録を見てから呼ぶ AnimalEvents.fertilizeEgg を通して、
    // プラグインが無いときは乱数列が vanilla と同じになるようにする。
    if (!dev.shifu.event.AnimalEvents.fertilizeEgg(this, other)) {
        return;
    }
    // Paper end - Add EntityFertilizeEggEvent event
