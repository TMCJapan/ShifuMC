# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# ServerTickStartEvent
file: net/minecraft/server/MinecraftServer.java
anchor:
    this.tickCount++;
insert:
    // Shifu - ServerTickEndEvent が読む。登録が無いときは時刻も取らない
    // (tick の途中で登録されると、その tick の長さだけ 0 からの差になる)
    if (dev.shifu.event.EventGuard.listening(com.destroystokyo.paper.event.server.ServerTickEndEvent.class)) {
        this.currentTickStart = System.nanoTime();
    }
    if (dev.shifu.event.EventGuard.listening(com.destroystokyo.paper.event.server.ServerTickStartEvent.class)) {
        new com.destroystokyo.paper.event.server.ServerTickStartEvent(this.tickCount+1).callEvent(); // Paper - Server Tick Events
    }

# ServerTickEndEvent
file: net/minecraft/server/MinecraftServer.java
anchor:
    this.profiler.push("tallying");
insert:
    // Paper start - Server Tick Events
    if (dev.shifu.event.EventGuard.listening(com.destroystokyo.paper.event.server.ServerTickEndEvent.class)) {
        // Shifu - 局所変数にしない。公式の tickServer にも long があると @Local long の候補が増えて mixin が当たらない
        dev.shifu.event.ShifuEvents.serverTickEnd(this.tickCount, this.currentTickStart, this.nextTickTimeNanos);
    }
    // Paper end - Server Tick Events
