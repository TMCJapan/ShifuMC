# VehicleDamageEvent と VehicleDestroyEvent(トロッコ)。
# 1.19.4 に VehicleEntity は無いので、舟とトロッコに分けて置く。
file: net/minecraft/world/entity/vehicle/AbstractMinecart.java
anchor:
            this.setHurtDir(-this.getHurtDir());
            this.setHurtTime(10);
            this.markHurt();
insert:
    if (!dev.shifu.event.EntityEvents.vehicleDamage(this, source, amount)) {
        return false;
    }

file: net/minecraft/world/entity/vehicle/AbstractMinecart.java
anchor:
                    this.destroy(source);
insert:
    if (!dev.shifu.event.EntityEvents.vehicleDestroy(this, source)) {
        return true;
    }
