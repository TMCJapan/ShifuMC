# main(26.2)の規則を reanchor.py で局所変数の名前だけ当て直したもの。

# Paper のパッチから作った発火。tools/make_events.py が出したもの。

# callEntityInteractEvent
file: net/minecraft/world/entity/ai/goal/RemoveBlockGoal.java
anchor:
    level.removeBlock(blockPos2, false);
insert:
    // CraftBukkit start - Step on eggs
    if (!org.bukkit.craftbukkit.event.CraftEventFactory.callEntityInteractEvent(this.removerMob, org.bukkit.craftbukkit.block.CraftBlock.at(level, blockPos2))) {
        return;
    }
    // CraftBukkit end
