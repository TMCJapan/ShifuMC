# PlayerNameEntityEvent。名札で名前を付ける直前。

file: net/minecraft/world/item/NameTagItem.java
anchor:
                entity.setCustomName(stack.getHoverName());
insert:
    if (!dev.shifu.event.PlayerEvents.nameEntity(user, entity, stack.getHoverName())) {
        return InteractionResult.PASS;
    }
