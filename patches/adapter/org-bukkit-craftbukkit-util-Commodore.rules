# アダプタ層の書き換え。vanilla の行ではない。
#
# 1.19.4 では、パッチの当て直し(tools/apply_classic_by_file.py)で
# 「Paper start - Rewrite plugins」の visitMethodInsn 向けの 3 つの書き換えが
# visitFieldInsn の中に入ってしまうので、置き直していた。
# 1.18.2 の Commodore にその 3 つ(getPlayerProfile・Advancement#getDisplay・
# WorldCreator#keepSpawnLoaded)は無く、visitFieldInsn / visitMethodInsn の
# どちらも Paper のままで通る。
