# アダプタ層の書き換え。vanilla の行ではない。

file: org/bukkit/craftbukkit/entity/CraftAllay.java

replace:
    Allay nmsAllay = this.getHandle().duplicateAllay();
with:
    Allay nmsAllay = this.getHandle().shifuDuplicateAllay(); // Shifu - vanilla の duplicateAllay は値を返さない
