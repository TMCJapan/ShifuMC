# アダプタ層の書き換え。vanilla の行ではない。

file: org/bukkit/craftbukkit/block/CraftChest.java

# 元案の ChestBlock.getMenuProvider 4 引数版(hand)は落とした
replace:
            MenuProvider provider = block.getMenuProvider(this.block, world.getHandle(), this.getPosition(), true);
    
            if (provider instanceof CraftInventoryDoubleChest.Provider doubleChestProvider) {
                inventory = new CraftInventoryDoubleChest(doubleChestProvider);
with:
            net.minecraft.world.Container container = ChestBlock.getContainer(block, this.block, world.getHandle(), this.getPosition(), true);
    
            if (container instanceof net.minecraft.world.CompoundContainer compoundContainer) {
                inventory = new CraftInventoryDoubleChest(compoundContainer);
