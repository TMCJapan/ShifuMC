# アダプタ層の書き換え。vanilla の行ではない(Paper が持つ vanilla のファイル)。
#
# 1.19.4 では catch (IOException) が「投げられない」で止まるので Exception へ広げていた。
# 1.18.2 の vanilla の ChunkStorage.read(ChunkPos) は throws IOException のままで、
# その呼び出しが同じ try の中にある(WorldUpgrader.java:136)。catch はそのまま通る。
