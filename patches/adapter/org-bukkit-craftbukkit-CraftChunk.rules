# アダプタ層の書き換え。vanilla の行ではない。
#
# 1.19.4 では CraftChunk の this.worldServer = chunk.level に ServerLevel の絞りを
# 入れていた。1.18.2 の Paper は
# 「this.worldServer = (ServerLevel) this.getHandle().level;」と自分で絞っているので要らない。
