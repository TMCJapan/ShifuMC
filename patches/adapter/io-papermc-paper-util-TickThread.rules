# アダプタ層の書き換え。vanilla の行ではない。
#
# 1.18.2 の io.papermc.paper.util.TickThread は isTickThread() / isTickThreadFor() を
# 持たない。メインスレッドの判定は Bukkit.isPrimaryThread() で、そちらは
# CraftServer 経由で MinecraftServer.isSameThread() を見るので、vanilla の
# サーバースレッドでもそのまま通る。1.19.4 で要った書き換えは 1.18.2 では要らない。
