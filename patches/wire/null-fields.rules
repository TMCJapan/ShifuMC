# 足したが誰も代入しない欄への配線。tools/make_wires.py が docs/backlog/null-fields.txt と
# Paper のパッチから出す。手で直さない。

# ServerPlayer.maxHealthCache(Paper が同じ位置で代入する)
file: net/minecraft/server/level/ServerPlayer.java
anchor:
    this.object = null;
insert-after:
    this.maxHealthCache = this.getMaxHealth();

# Creeper.entityIgniter(Paper が同じ位置で代入する)
file: net/minecraft/world/entity/monster/Creeper.java
anchor:
    if (!this.level().isClientSide()) {
insert-after:
    this.entityIgniter = player;

# AbstractFurnaceMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/AbstractFurnaceMenu.java
anchor:
    this.addSlot(new FurnaceResultSlot(playerInventory.player, inventory, 2, 116, 35));
insert-after:
    this.inventory = playerInventory;

# AbstractMountInventoryMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/AbstractMountInventoryMenu.java
anchor:
    super(null, containerId);
insert-after:
    this.inventory = playerInventory;

# BeaconMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/BeaconMenu.java
anchor:
    super(MenuType.BEACON, syncId);
insert-after:
    this.inventory = (net.minecraft.world.entity.player.Inventory) container;

# BrewingStandMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/BrewingStandMenu.java
anchor:
    super(MenuType.BREWING_STAND, syncId);
insert-after:
    this.inventory = playerInventory;

# CartographyTableMenu.player(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/CartographyTableMenu.java
anchor:
    this.addStandardInventorySlots(playerInventory, 8, 84);
insert-after:
    this.player = (org.bukkit.entity.Player) playerInventory.player.getBukkitEntity();

# DispenserMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/DispenserMenu.java
anchor:
    super(MenuType.GENERIC_3x3, syncId);
insert-after:
    this.inventory = inventory;

# EnchantmentMenu.player(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/EnchantmentMenu.java
anchor:
    this.addDataSlot(DataSlot.shared(this.levelClue, 2));
insert-after:
    this.player = (org.bukkit.entity.Player) playerInventory.player.getBukkitEntity();

# GrindstoneMenu.player(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/GrindstoneMenu.java
anchor:
    this.addStandardInventorySlots(playerInventory, 8, 84);
insert-after:
    this.player = (org.bukkit.entity.Player) playerInventory.player.getBukkitEntity();

# HopperMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/HopperMenu.java
anchor:
    this.hopper = container;
insert-after:
    this.inventory = inventory;

# LoomMenu.player(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/LoomMenu.java
anchor:
    this.patternGetter = playerInventory.player.registryAccess().lookupOrThrow(Registries.BANNER_PATTERN);
insert-after:
    this.player = (org.bukkit.entity.Player) playerInventory.player.getBukkitEntity();

# MerchantMenu.inventory(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/MerchantMenu.java
anchor:
    this.addSlot(new MerchantResultSlot(playerInventory.player, merchant, this.tradeContainer, 2, 220, 37));
insert-after:
    this.inventory = playerInventory;

# StonecutterMenu.player(Paper が同じ位置で代入する)
file: net/minecraft/world/inventory/StonecutterMenu.java
anchor:
    this.addDataSlot(this.selectedRecipeIndex);
insert-after:
    this.player = (org.bukkit.entity.Player) playerInventory.player.getBukkitEntity();
