# 逆コンパイルで消えた局所変数を戻す。作り方は tools/make_decompile_rules.py
#
# Mojang の元のソースは値をいったん変数に入れてから instanceof で見ている。
# 逆コンパイラが 1 行にまとめると変数が消え、@Local で捕まえる MOD が当たらない。
# 式は同じ回数だけ、同じ順で評価されるので、挙動は変わらない。

file: net/minecraft/core/dispenser/DispenseItemBehavior.java
replace:
    if (level.getBlockEntity(target) instanceof SkullBlockEntity skullBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity skull = level.getBlockEntity(target);
    if (skull instanceof SkullBlockEntity skullBlockEntity) {

file: net/minecraft/core/dispenser/ShearsDispenseItemBehavior.java
replace:
    for (Entity entity : level.getEntitiesOfClass(Entity.class, new AABB(pos), EntitySelector.NO_SPECTATORS)) {
with:
    var entities = level.getEntitiesOfClass(Entity.class, new AABB(pos), EntitySelector.NO_SPECTATORS);
    for (Entity entity : entities) {

file: net/minecraft/core/dispenser/ShulkerBoxDispenseBehavior.java
replace:
    if (dispensed.getItem() instanceof BlockItem blockItem) {
with:
    net.minecraft.world.item.Item item = dispensed.getItem();
    if (item instanceof BlockItem blockItem) {

file: net/minecraft/server/MinecraftServer.java
replace:
    for (ServerPlayer player : Lists.newArrayList(playerList.getPlayers())) {
with:
    var players = Lists.newArrayList(playerList.getPlayers());
    for (ServerPlayer player : players) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.level().getBlockEntity(blockPos) instanceof StructureBlockEntity structure) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level().getBlockEntity(blockPos);
    if (blockEntity instanceof StructureBlockEntity structure) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.level().getBlockEntity(blockPos) instanceof TestBlockEntity testBlock) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level().getBlockEntity(blockPos);
    if (blockEntity instanceof TestBlockEntity testBlock) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    BlockState state = this.player.level().getBlockState(blockPos);
    if (this.player.level().getBlockEntity(blockPos) instanceof JigsawBlockEntity jigsaw) {
with:
    BlockState state = this.player.level().getBlockState(blockPos);
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level().getBlockEntity(blockPos);
    if (blockEntity instanceof JigsawBlockEntity jigsaw) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    BlockPos blockPos = packet.getPos();
    if (this.player.level().getBlockEntity(blockPos) instanceof JigsawBlockEntity jigsaw) {
with:
    BlockPos blockPos = packet.getPos();
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level().getBlockEntity(blockPos);
    if (blockEntity instanceof JigsawBlockEntity jigsaw) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.getControlledVehicle() instanceof AbstractBoat boat) {
with:
    net.minecraft.world.entity.Entity vehicle = this.player.getControlledVehicle();
    if (vehicle instanceof AbstractBoat boat) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.interactOn(target, hand, location) instanceof InteractionResult.Success success) {
with:
    net.minecraft.world.InteractionResult result = this.player.interactOn(target, hand, location);
    if (result instanceof InteractionResult.Success success) {

file: net/minecraft/server/players/PlayerList.java
replace:
    for (Packet<?> packet : scoreboard.getStartTrackingPackets(objective)) {
with:
    var packets = scoreboard.getStartTrackingPackets(objective);
    for (Packet<?> packet : packets) {

file: net/minecraft/server/players/PlayerList.java
replace:
    for (String name : team.getPlayers()) {
with:
    var teamPlayers = team.getPlayers();
    for (String name : teamPlayers) {

file: net/minecraft/world/RandomizableContainer.java
replace:
    if (level.getBlockEntity(blockEntityPos) instanceof RandomizableContainer randomizableContainer) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(blockEntityPos);
    if (blockEntity instanceof RandomizableContainer randomizableContainer) {

file: net/minecraft/world/entity/Entity.java
replace:
    for (double coord : collider.getCoords(Direction.Axis.Y)) {
with:
    var coords = collider.getCoords(Direction.Axis.Y);
    for (double coord : coords) {

file: net/minecraft/world/entity/ExperienceOrb.java
replace:
    for (ExperienceOrb orb : this.level().getEntities(EntityTypeTest.forClass(ExperienceOrb.class), this.getBoundingBox().inflate(0.5), this::canMerge)) {
with:
    var orbs = this.level().getEntities(EntityTypeTest.forClass(ExperienceOrb.class), this.getBoundingBox().inflate(0.5), this::canMerge);
    for (ExperienceOrb orb : orbs) {

file: net/minecraft/world/entity/LivingEntity.java
replace:
    if (entity.getOwner() instanceof ServerPlayer serverPlayer) {
with:
    net.minecraft.world.entity.Entity thrower = entity.getOwner();
    if (thrower instanceof ServerPlayer serverPlayer) {

file: net/minecraft/world/entity/animal/equine/TraderLlama.java
replace:
    if (this.llama.getLeashHolder() instanceof WanderingTrader wanderingTrader) {
with:
    net.minecraft.world.entity.Entity leashHolder = this.llama.getLeashHolder();
    if (leashHolder instanceof WanderingTrader wanderingTrader) {

file: net/minecraft/world/entity/animal/feline/Cat.java
replace:
    for (Cat otherCat : this.cat.level().getEntitiesOfClass(Cat.class, new AABB(this.goalPos).inflate(2.0))) {
with:
    var cats = this.cat.level().getEntitiesOfClass(Cat.class, new AABB(this.goalPos).inflate(2.0));
    for (Cat otherCat : cats) {

file: net/minecraft/world/entity/animal/feline/Cat.java
replace:
    for (Player player : this.level().getEntitiesOfClass(Player.class, new AABB(catPos).inflate(2.0, 2.0, 2.0))) {
with:
    var players = this.level().getEntitiesOfClass(Player.class, new AABB(catPos).inflate(2.0, 2.0, 2.0));
    for (Player player : players) {

file: net/minecraft/world/entity/animal/nautilus/AbstractNautilus.java
replace:
    if (this.getFirstPassenger() instanceof Player player) {
with:
    net.minecraft.world.entity.Entity passenger = this.getFirstPassenger();
    if (passenger instanceof Player player) {

file: net/minecraft/world/entity/animal/polarbear/PolarBear.java
replace:
    for (PolarBear bear : PolarBear.this.level().getEntitiesOfClass(PolarBear.class, PolarBear.this.getBoundingBox().inflate(8.0, 4.0, 8.0))) {
with:
    var bears = PolarBear.this.level().getEntitiesOfClass(PolarBear.class, PolarBear.this.getBoundingBox().inflate(8.0, 4.0, 8.0));
    for (PolarBear bear : bears) {

file: net/minecraft/world/entity/decoration/ArmorStand.java
replace:
    for (Entity entity : this.level().getEntities(this, this.getBoundingBox(), RIDABLE_MINECARTS)) {
with:
    var entities = this.level().getEntities(this, this.getBoundingBox(), RIDABLE_MINECARTS);
    for (Entity entity : entities) {

file: net/minecraft/world/entity/decoration/LeashFenceKnotEntity.java
replace:
    for (Leashable mob : Leashable.leashableLeashedTo(this)) {
with:
    var knotLeashable = Leashable.leashableLeashedTo(this);
    for (Leashable mob : knotLeashable) {

file: net/minecraft/world/entity/decoration/LeashFenceKnotEntity.java
replace:
    for (LeashFenceKnotEntity knot : level.getEntitiesOfClass(LeashFenceKnotEntity.class, new AABB(x - 1.0, y - 1.0, z - 1.0, x + 1.0, y + 1.0, z + 1.0))) {
with:
    var knots = level.getEntitiesOfClass(LeashFenceKnotEntity.class, new AABB(x - 1.0, y - 1.0, z - 1.0, x + 1.0, y + 1.0, z + 1.0));
    for (LeashFenceKnotEntity knot : knots) {

file: net/minecraft/world/entity/monster/EnderMan.java
replace:
    for (ItemStack itemStack : carryingBlock.getDrops(params)) {
with:
    var blockDrops = carryingBlock.getDrops(params);
    for (ItemStack itemStack : blockDrops) {

file: net/minecraft/world/entity/monster/Ravager.java
replace:
    for (LivingEntity entity : this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(4.0), targetSelector)) {
with:
    var entities = this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(4.0), targetSelector);
    for (LivingEntity entity : entities) {

file: net/minecraft/world/entity/monster/Ravager.java
replace:
    for (LivingEntity entity : this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(4.0), ROAR_TARGET_ON_CLIENT)) {
with:
    var entities = this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(4.0), ROAR_TARGET_ON_CLIENT);
    for (LivingEntity entity : entities) {

file: net/minecraft/world/entity/monster/Silverfish.java
replace:
    if (blockState.getBlock() instanceof InfestedBlock infestedBlock) {
with:
    net.minecraft.world.level.block.Block block = blockState.getBlock();
    if (block instanceof InfestedBlock infestedBlock) {

file: net/minecraft/world/entity/projectile/FireworkRocketEntity.java
replace:
    for (LivingEntity target : this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(5.0))) {
with:
    var targets = this.level().getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(5.0));
    for (LivingEntity target : targets) {

file: net/minecraft/world/entity/projectile/FireworkRocketEntity.java
replace:
    if (this.level().getEntity(id) instanceof LivingEntity livingEntity) {
with:
    net.minecraft.world.entity.Entity ent = this.level().getEntity(id);
    if (ent instanceof LivingEntity livingEntity) {

file: net/minecraft/world/entity/projectile/throwableitemprojectile/AbstractThrownPotion.java
replace:
    for (LivingEntity entity : this.level().getEntitiesOfClass(LivingEntity.class, aabb, WATER_SENSITIVE_OR_ON_FIRE)) {
with:
    var burningOrWaterSensitiveEntities = this.level().getEntitiesOfClass(LivingEntity.class, aabb, WATER_SENSITIVE_OR_ON_FIRE);
    for (LivingEntity entity : burningOrWaterSensitiveEntities) {

file: net/minecraft/world/entity/vehicle/ContainerEntity.java
replace:
    if (source.getDirectEntity() instanceof Player player) {
with:
    net.minecraft.world.entity.Entity directEntity = source.getDirectEntity();
    if (directEntity instanceof Player player) {

file: net/minecraft/world/item/MinecartItem.java
replace:
    for (Entity entity : level.getEntities(null, cart.getBoundingBox())) {
with:
    var entities = level.getEntities(null, cart.getBoundingBox());
    for (Entity entity : entities) {

file: net/minecraft/world/item/enchantment/EnchantmentHelper.java
replace:
    if (killingBlow.getEntity() instanceof LivingEntity livingAttacker) {
with:
    net.minecraft.world.entity.Entity attacker = killingBlow.getEntity();
    if (attacker instanceof LivingEntity livingAttacker) {

file: net/minecraft/world/level/ServerExplosion.java
replace:
    for (Entity entity : this.level.getEntities(this.source, new AABB(x0, y0, z0, x1, y1, z1))) {
with:
    var entities = this.level.getEntities(this.source, new AABB(x0, y0, z0, x1, y1, z1));
    for (Entity entity : entities) {

file: net/minecraft/world/level/block/BeehiveBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof BeehiveBlockEntity beehiveBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof BeehiveBlockEntity beehiveBlockEntity) {

file: net/minecraft/world/level/block/Block.java
replace:
    for (Entity collidingEntity : level.getEntities(null, offsetShape.bounds())) {
with:
    var collidingEntities = level.getEntities(null, offsetShape.bounds());
    for (Entity collidingEntity : collidingEntities) {

file: net/minecraft/world/level/block/CampfireBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof CampfireBlockEntity campfire) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof CampfireBlockEntity campfire) {

file: net/minecraft/world/level/block/ChestBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof ChestBlockEntity chestBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof ChestBlockEntity chestBlockEntity) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    if (!level.isClientSide()) {
    if (level.getBlockEntity(pos) instanceof CommandBlockEntity commandBlock) {
with:
    if (!level.isClientSide()) {
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlock) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    protected void tick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
    if (level.getBlockEntity(pos) instanceof CommandBlockEntity commandBlock) {
with:
    protected void tick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlock) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof CommandBlockEntity commandBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlockEntity) {

file: net/minecraft/world/level/block/DetectorRailBlock.java
replace:
    for (BlockPos connectionPos : rail.getConnections()) {
with:
    var connections = rail.getConnections();
    for (BlockPos connectionPos : connections) {

file: net/minecraft/world/level/block/EnchantingTableBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof EnchantingTableBlockEntity enchantingTable) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof EnchantingTableBlockEntity enchantingTable) {

file: net/minecraft/world/level/block/EndGatewayBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof TheEndGatewayBlockEntity theEndGatewayBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof TheEndGatewayBlockEntity theEndGatewayBlockEntity) {

file: net/minecraft/world/level/block/EndGatewayBlock.java
replace:
    if (currentLevel.getBlockEntity(portalEntryPos) instanceof TheEndGatewayBlockEntity endGatewayBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = currentLevel.getBlockEntity(portalEntryPos);
    if (blockEntity instanceof TheEndGatewayBlockEntity endGatewayBlockEntity) {

file: net/minecraft/world/level/block/HopperBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof HopperBlockEntity hopperBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof HopperBlockEntity hopperBlockEntity) {

file: net/minecraft/world/level/block/LecternBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof LecternBlockEntity lectern) {
with:
    net.minecraft.world.level.block.entity.BlockEntity entity = level.getBlockEntity(pos);
    if (entity instanceof LecternBlockEntity lectern) {

file: net/minecraft/world/level/block/LecternBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof LecternBlockEntity lecternBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof LecternBlockEntity lecternBlockEntity) {

file: net/minecraft/world/level/block/SculkSensorBlock.java
replace:
    if (level.getBlockEntity(pos) instanceof SculkSensorBlockEntity sculk) {
with:
    net.minecraft.world.level.block.entity.BlockEntity entity = level.getBlockEntity(pos);
    if (entity instanceof SculkSensorBlockEntity sculk) {

file: net/minecraft/world/level/block/TargetBlock.java
replace:
    if (projectile.getOwner() instanceof ServerPlayer playerOwner) {
with:
    net.minecraft.world.entity.Entity owner = projectile.getOwner();
    if (owner instanceof ServerPlayer playerOwner) {

file: net/minecraft/world/level/block/entity/BeaconBlockEntity.java
replace:
    if (state.getBlock() instanceof BeaconBeamBlock beaconBeamBlock) {
with:
    net.minecraft.world.level.block.Block block = state.getBlock();
    if (block instanceof BeaconBeamBlock beaconBeamBlock) {

file: net/minecraft/world/level/block/entity/CreakingHeartBlockEntity.java
replace:
    if (serverLevel.getEntity(uuid) instanceof Creaking resolvedCreaking) {
with:
    net.minecraft.world.entity.Entity entity = serverLevel.getEntity(uuid);
    if (entity instanceof Creaking resolvedCreaking) {

file: net/minecraft/world/level/block/entity/SculkCatalystBlockEntity.java
replace:
    if (mob.getLastHurtByMob() instanceof ServerPlayer player) {
with:
    net.minecraft.world.entity.LivingEntity lastHurtByMob = mob.getLastHurtByMob();
    if (lastHurtByMob instanceof ServerPlayer player) {

file: net/minecraft/world/level/chunk/LevelChunk.java
replace:
    private <T extends BlockEntity> void removeGameEventListener(final T blockEntity, final ServerLevel level) {
    if (blockEntity.getBlockState().getBlock() instanceof EntityBlock entityBlock) {
with:
    private <T extends BlockEntity> void removeGameEventListener(final T blockEntity, final ServerLevel level) {
    net.minecraft.world.level.block.Block block = blockEntity.getBlockState().getBlock();
    if (block instanceof EntityBlock entityBlock) {

file: net/minecraft/world/level/chunk/LevelChunk.java
replace:
    private <T extends BlockEntity> void addGameEventListener(final T blockEntity, final ServerLevel level) {
    if (blockEntity.getBlockState().getBlock() instanceof EntityBlock entityBlock) {
with:
    private <T extends BlockEntity> void addGameEventListener(final T blockEntity, final ServerLevel level) {
    net.minecraft.world.level.block.Block block = blockEntity.getBlockState().getBlock();
    if (block instanceof EntityBlock entityBlock) {

file: net/minecraft/world/level/dimension/end/EnderDragonFight.java
replace:
    if (this.level.getEntity(this.dragonUUID) instanceof EnderDragon actuallyDragon) {
with:
    net.minecraft.world.entity.Entity dragon = this.level.getEntity(this.dragonUUID);
    if (dragon instanceof EnderDragon actuallyDragon) {

file: net/minecraft/world/level/dimension/end/EnderDragonFight.java
replace:
    for (EndCrystal crystal : this.level.getEntitiesOfClass(EndCrystal.class, spike.getTopBoundingBox())) {
with:
    var spikeCrystals = this.level.getEntitiesOfClass(EndCrystal.class, spike.getTopBoundingBox());
    for (EndCrystal crystal : spikeCrystals) {

file: net/minecraft/world/level/levelgen/structure/StructurePiece.java
replace:
    if (level.getBlockEntity(pos) instanceof ChestBlockEntity chestBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof ChestBlockEntity chestBlockEntity) {

file: net/minecraft/world/level/levelgen/structure/StructurePiece.java
replace:
    if (level.getBlockEntity(pos) instanceof DispenserBlockEntity dispenserBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = level.getBlockEntity(pos);
    if (blockEntity instanceof DispenserBlockEntity dispenserBlockEntity) {
