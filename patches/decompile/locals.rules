# 逆コンパイルで消えた局所変数を戻す。作り方は tools/make_decompile_rules.py
#
# Mojang の元のソースは値をいったん変数に入れてから instanceof で見ている。
# 逆コンパイラが 1 行にまとめると変数が消え、@Local で捕まえる MOD が当たらない。
# 式は同じ回数だけ、同じ順で評価されるので、挙動は変わらない。

file: net/minecraft/core/dispenser/ShearsDispenseItemBehavior.java
replace:
    for (LivingEntity livingEntity : world.getEntitiesOfClass(LivingEntity.class, new AABB(pos), EntitySelector.NO_SPECTATORS)) {
with:
    java.util.List<? extends LivingEntity> list = world.getEntitiesOfClass(LivingEntity.class, new AABB(pos), EntitySelector.NO_SPECTATORS);
    for (LivingEntity livingEntity : list) {

file: net/minecraft/server/MinecraftServer.java
replace:
    for (ServerPlayer serverPlayer : Lists.newArrayList(playerList.getPlayers())) {
with:
    java.util.List<? extends ServerPlayer> list = Lists.newArrayList(playerList.getPlayers());
    for (ServerPlayer serverPlayer : list) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.level.getBlockEntity(pos) instanceof StructureBlockEntity structureBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level.getBlockEntity(pos);
    if (blockEntity instanceof StructureBlockEntity structureBlockEntity) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    BlockState blockState = this.player.level.getBlockState(pos);
    if (this.player.level.getBlockEntity(pos) instanceof JigsawBlockEntity jigsawBlockEntity) {
with:
    BlockState blockState = this.player.level.getBlockState(pos);
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level.getBlockEntity(pos);
    if (blockEntity instanceof JigsawBlockEntity jigsawBlockEntity) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    BlockPos pos = packet.getPos();
    if (this.player.level.getBlockEntity(pos) instanceof JigsawBlockEntity jigsawBlockEntity) {
with:
    BlockPos pos = packet.getPos();
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = this.player.level.getBlockEntity(pos);
    if (blockEntity instanceof JigsawBlockEntity jigsawBlockEntity) {

file: net/minecraft/server/network/ServerGamePacketListenerImpl.java
replace:
    if (this.player.containerMenu instanceof MerchantMenu merchantMenu) {
with:
    net.minecraft.world.inventory.AbstractContainerMenu abstractContainerMenu = this.player.containerMenu;
    if (abstractContainerMenu instanceof MerchantMenu merchantMenu) {

file: net/minecraft/server/players/PlayerList.java
replace:
    for (Packet<?> packet : scoreboard.getStartTrackingPackets(displayObjective)) {
with:
    java.util.List<? extends Packet<?>> list = scoreboard.getStartTrackingPackets(displayObjective);
    for (Packet<?> packet : list) {

file: net/minecraft/server/players/PlayerList.java
replace:
    for (String string : team.getPlayers()) {
with:
    java.util.Collection<? extends String> collection = team.getPlayers();
    for (String string : collection) {

file: net/minecraft/world/entity/LivingEntity.java
replace:
    for (Pair<MobEffectInstance, Float> pair : item.getFoodProperties().getEffects()) {
with:
    java.util.List<? extends Pair<MobEffectInstance, Float>> list = item.getFoodProperties().getEffects();
    for (Pair<MobEffectInstance, Float> pair : list) {

file: net/minecraft/world/entity/Mob.java
replace:
    for (ItemEntity itemEntity : this.level.getEntitiesOfClass(ItemEntity.class, this.getBoundingBox().inflate(1.0, 0.0, 1.0))) {
with:
    java.util.List<? extends ItemEntity> list = this.level.getEntitiesOfClass(ItemEntity.class, this.getBoundingBox().inflate(1.0, 0.0, 1.0));
    for (ItemEntity itemEntity : list) {

file: net/minecraft/world/entity/animal/Bee.java
replace:
    if (Bee.this.level.getBlockEntity(Bee.this.hivePos) instanceof BeehiveBlockEntity beehiveBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = Bee.this.level.getBlockEntity(Bee.this.hivePos);
    if (blockEntity instanceof BeehiveBlockEntity beehiveBlockEntity) {

file: net/minecraft/world/entity/animal/Cat.java
replace:
    for (Cat cat : this.cat.level.getEntitiesOfClass(Cat.class, new AABB(this.goalPos).inflate(2.0))) {
with:
    java.util.List<? extends Cat> list = this.cat.level.getEntitiesOfClass(Cat.class, new AABB(this.goalPos).inflate(2.0));
    for (Cat cat : list) {

file: net/minecraft/world/entity/animal/Cat.java
replace:
    for (ItemStack itemStack : lootTable.getRandomItems(builder.create(LootContextParamSets.GIFT))) {
with:
    java.util.List<? extends ItemStack> list = lootTable.getRandomItems(builder.create(LootContextParamSets.GIFT));
    for (ItemStack itemStack : list) {

file: net/minecraft/world/entity/animal/Panda.java
replace:
    for (Panda panda : this.level.getEntitiesOfClass(Panda.class, this.getBoundingBox().inflate(10.0))) {
with:
    java.util.List<? extends Panda> list = this.level.getEntitiesOfClass(Panda.class, this.getBoundingBox().inflate(10.0));
    for (Panda panda : list) {

file: net/minecraft/world/entity/animal/Pufferfish.java
replace:
    for (Mob mob : this.level.getEntitiesOfClass(Mob.class, this.getBoundingBox().inflate(0.3), entity -> targetingConditions.test(this, entity))) {
with:
    java.util.List<? extends Mob> list = this.level.getEntitiesOfClass(Mob.class, this.getBoundingBox().inflate(0.3), entity -> targetingConditions.test(this, entity));
    for (Mob mob : list) {

file: net/minecraft/world/entity/monster/piglin/PiglinAi.java
replace:
    for (ItemStack itemStack : entity.getArmorSlots()) {
with:
    java.lang.Iterable<? extends ItemStack> iterable = entity.getArmorSlots();
    for (ItemStack itemStack : iterable) {

file: net/minecraft/world/entity/player/Player.java
replace:
    for (LivingEntity livingEntity : this.level.getEntitiesOfClass(LivingEntity.class, target.getBoundingBox().inflate(1.0, 0.25, 1.0))) {
with:
    java.util.List<? extends LivingEntity> list = this.level.getEntitiesOfClass(LivingEntity.class, target.getBoundingBox().inflate(1.0, 0.25, 1.0));
    for (LivingEntity livingEntity : list) {

file: net/minecraft/world/entity/projectile/FireworkRocketEntity.java
replace:
    for (LivingEntity livingEntity : this.level.getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(5.0))) {
with:
    java.util.List<? extends LivingEntity> list = this.level.getEntitiesOfClass(LivingEntity.class, this.getBoundingBox().inflate(5.0));
    for (LivingEntity livingEntity : list) {

file: net/minecraft/world/entity/projectile/ThrownPotion.java
replace:
    for (Axolotl axolotl : this.level.getEntitiesOfClass(Axolotl.class, aabb)) {
with:
    java.util.List<? extends Axolotl> list2 = this.level.getEntitiesOfClass(Axolotl.class, aabb);
    for (Axolotl axolotl : list2) {

file: net/minecraft/world/item/LeadItem.java
replace:
    for (Mob mob : world.getEntitiesOfClass(Mob.class, new AABB(x - 7.0, y - 7.0, z - 7.0, x + 7.0, y + 7.0, z + 7.0))) {
with:
    java.util.List<? extends Mob> list = world.getEntitiesOfClass(Mob.class, new AABB(x - 7.0, y - 7.0, z - 7.0, x + 7.0, y + 7.0, z + 7.0));
    for (Mob mob : list) {

file: net/minecraft/world/level/block/BeehiveBlock.java
replace:
    if (world.getBlockEntity(pos) instanceof BeehiveBlockEntity beehiveBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = world.getBlockEntity(pos);
    if (blockEntity instanceof BeehiveBlockEntity beehiveBlockEntity) {

file: net/minecraft/world/level/block/Block.java
replace:
    for (Entity entity : world.getEntities(null, voxelShape.bounds())) {
with:
    java.util.List<? extends Entity> list = world.getEntities(null, voxelShape.bounds());
    for (Entity entity : list) {

file: net/minecraft/world/level/block/CampfireBlock.java
replace:
    if (world.getBlockEntity(pos) instanceof CampfireBlockEntity campfireBlockEntity) {
with:
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = world.getBlockEntity(pos);
    if (blockEntity instanceof CampfireBlockEntity campfireBlockEntity) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    if (!world.isClientSide) {
    if (world.getBlockEntity(pos) instanceof CommandBlockEntity commandBlockEntity) {
with:
    if (!world.isClientSide) {
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = world.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlockEntity) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    public void tick(BlockState state, ServerLevel world, BlockPos pos, Random random) {
    if (world.getBlockEntity(pos) instanceof CommandBlockEntity commandBlockEntity) {
with:
    public void tick(BlockState state, ServerLevel world, BlockPos pos, Random random) {
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = world.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlockEntity) {

file: net/minecraft/world/level/block/CommandBlock.java
replace:
    public void setPlacedBy(Level world, BlockPos pos, BlockState state, LivingEntity placer, ItemStack itemStack) {
    if (world.getBlockEntity(pos) instanceof CommandBlockEntity commandBlockEntity) {
with:
    public void setPlacedBy(Level world, BlockPos pos, BlockState state, LivingEntity placer, ItemStack itemStack) {
    net.minecraft.world.level.block.entity.BlockEntity blockEntity = world.getBlockEntity(pos);
    if (blockEntity instanceof CommandBlockEntity commandBlockEntity) {

file: net/minecraft/world/level/block/DetectorRailBlock.java
replace:
    for (BlockPos blockPos : railState.getConnections()) {
with:
    java.util.List<? extends BlockPos> list = railState.getConnections();
    for (BlockPos blockPos : list) {

file: net/minecraft/world/level/block/TargetBlock.java
replace:
    if (projectile.getOwner() instanceof ServerPlayer serverPlayer) {
with:
    net.minecraft.world.entity.Entity entity = projectile.getOwner();
    if (entity instanceof ServerPlayer serverPlayer) {

file: net/minecraft/world/level/dimension/end/EndDragonFight.java
replace:
    for (EndCrystal endCrystal : this.level.getEntitiesOfClass(EndCrystal.class, endSpike.getTopBoundingBox())) {
with:
    java.util.List<? extends EndCrystal> list = this.level.getEntitiesOfClass(EndCrystal.class, endSpike.getTopBoundingBox());
    for (EndCrystal endCrystal : list) {

file: net/minecraft/world/level/storage/LevelStorageSource.java
replace:
    if (readLightweightData(file) instanceof CompoundTag compoundTag) {
with:
    net.minecraft.nbt.Tag tag = readLightweightData(file);
    if (tag instanceof CompoundTag compoundTag) {

file: net/minecraft/world/level/storage/LevelStorageSource.java
replace:
    if (readLightweightData(file1) instanceof CompoundTag compoundTag) {
with:
    net.minecraft.nbt.Tag tag = readLightweightData(file1);
    if (tag instanceof CompoundTag compoundTag) {
