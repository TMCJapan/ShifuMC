// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。

// EntitySelectorParser.parse
    public EntitySelector parse(final boolean overridePermissions) throws CommandSyntaxException {
        return this.parse();
    }
