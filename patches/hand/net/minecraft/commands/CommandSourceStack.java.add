// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。
//
// Paper は CommandSourceStack に PaperCommandSourceStack(API の CommandSourceStack)を実装させているが、
// Shifu は実装させない。API へ渡すときは dev.shifu.command.ApiSource で包む
// (docs/ARCHITECTURE.md の「Paper の brigadier API」)。ここの宣言には @Override を付けない。

// CommandSourceStack.getHandle
    // Paper のパッチと同じで、自分を返す。
    public CommandSourceStack getHandle() {
        return this;
    }

// CommandSourceStack.shifuApiSource
    // Paper の brigadier API に渡すときに包んだもの(dev.shifu.command.ApiSource)。
    // 同じ source を何度包んでも同じオブジェクトになるように、ここに覚えておく。
    // 型を Object にしているのは、vanilla のクラスからアダプタ層の型を参照しないため。
    public Object shifuApiSource;
