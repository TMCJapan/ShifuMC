// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。

// FlowingFluid.beforeDestroyingBlock
    // Paper は source(流れて来た位置)を足している。vanilla の 3 引数の版へ渡す
    protected void beforeDestroyingBlock(final LevelAccessor world, final BlockPos pos, final BlockState state,
                                         final BlockPos source) {
        this.beforeDestroyingBlock(world, pos, state);
    }
