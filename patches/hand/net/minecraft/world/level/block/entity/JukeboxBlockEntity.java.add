// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。

// JukeboxBlockEntity.setSongItemWithoutPlaying
    // ticksSinceSongStarted は途中から鳴らすための位置。vanilla は先頭からなので渡さない。
    public void setSongItemWithoutPlaying(final ItemStack itemStack, final long ticksSinceSongStarted) {
        this.setSongItemWithoutPlaying(itemStack);
    }
