// 自前で書いた追加。Paper の宣言だけを写す。
//
// 1.18.2 にスカルク一式(SculkSpreader)は無い。SculkBloomEvent のために置いていた
// level の欄を消した(patches/wire/null-fields-hand.rules と
// patches/events/generated/net-minecraft-world-level-block-SculkSpreader.rules も同じ)。
