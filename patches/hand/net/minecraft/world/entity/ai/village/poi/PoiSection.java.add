

// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。

// PoiSection.isEmpty
    // Paper のチャンク系(PoiChunk)が空の区画を書かないために読む
    public boolean isEmpty() {
        return this.isValid && this.records.isEmpty() && this.byType.isEmpty();
    }
