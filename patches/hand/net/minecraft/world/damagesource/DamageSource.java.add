// 自前で書いた追加。Paper の宣言だけを写し、本体は vanilla の版へ渡す。
//
// 1.19.4 から消した塊:
//   DamageSource(Holder<DamageType>, Entity, Entity, Vec3, Pointers) と damageContext
//     — データ駆動の DamageType は 1.19.4(Holder<DamageType>)からのもの。1.18.2 の
//       DamageSource は static の定数と protected DamageSource(String) だけで、
//       Paper 1.18.2 も構築子を足していない(足しているのは sweep()・critical() のような
//       自分を返すメソッドで、どれも make_classic_shim が本体ごと写せる)。
//       adventure の Pointers を持つ damageContext も 1.18.2 の Paper には無い。
